# Filled in during Sprint 1 — shared baseline for now
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
pgvector>=0.3
redis>=5.2
python-dotenv>=1.0
httpx>=0.28

# Data Engineer — synthetic data generation
faker>=30
dateparser>=1.2
jsonschema>=4.23
typer>=0.12

# LLM Engineer — OpenAI client, structured output, structured logging
openai>=1.50
instructor>=1.5
structlog>=24.1

# LLM Engineer — local embeddings (ADR-002). Pulls torch; grows the worker image.
sentence-transformers>=5.0
