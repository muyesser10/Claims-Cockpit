# schemas/

`claim.json` is the **single source of truth** for this project.

Everything below is derived from it:
- Backend Pydantic models
- LLM structured-output schema
- Frontend TypeScript types
- Eval normalization rules

**Frozen during the Day 2 schema session (09:30-11:30, Sprint 1).**
Any later change requires a `[SCHEMA CHANGE]`-tagged PR and team approval.
